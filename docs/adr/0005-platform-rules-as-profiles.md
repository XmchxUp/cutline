# Platform rules are profiles

Cutline will represent Douyin-oriented output requirements as a default PlatformProfile rather than hard-coding them into creative workflow logic. Douyin is the V1 target, but keeping aspect ratio, target duration, subtitles, safe area, hook, and ending requirements in a profile lets future platforms reuse the same creative pipeline with different publishing constraints.
