# Draft packages record step runs

Cutline draft packages will preserve auditable records for creative pipeline steps instead of storing only final generated outputs. Recording each step's inputs, outputs, provider, parameters, status, and errors makes poor AI results explainable and enables partial regeneration of a hook, narration, voiceover, visual match, preview, or render without throwing away the entire draft.
